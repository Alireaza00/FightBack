import Layout from "@/components/layout";
import BoundaryBuilder from "@/components/boundary-builder";

export default function BoundaryBuilderPage() {
  return (
    <Layout>
      <BoundaryBuilder />
    </Layout>
  );
}